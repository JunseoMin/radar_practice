#ifndef LOCALMAPPING_HPP
#define LOCALMAPPING_HPP

#include "radar_utills.hpp"

/**
 * @brief refine pose estimation and BA
 */
class LocalMapping{
public:
  LocalMapping() = default;
  LocalMapping();

private:

};

#endif // LOCALMAPPING_HPP
